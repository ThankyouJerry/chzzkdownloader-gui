# 변경 이력

ClipCatcher의 사용자에게 영향을 주는 변경 사항을 버전별로 기록합니다.

## [2.0.10] - 2026-08-20

### 수정

- 변경된 치지직 클립 API에 맞춰 상세 정보와 재생 정보를 병합하고, 실제 재생 API에서 화질과 서명된 다운로드 주소를 가져오도록 수정했습니다.
- 대기열에서 클립 다운로드가 시작될 때 만료 가능성이 있는 주소를 새로 받아 `H.264 + AAC` MP4로 처리합니다.
- 치지직 VOD 재생 API의 큰 JSON 응답이 여러 네트워크 조각으로 들어올 때 일부만 읽어 화질 조회가 실패하던 문제를 수정했습니다.
- YouTube 재생목록이 포함된 링크에서 선택한 영상 외 항목까지 처리될 수 있던 문제를 수정했습니다.
- 빠른 다시보기가 정식 VOD로 변환된 경우 오래된 HLS 주소를 만들지 않고 `yt-dlp` 방식으로 자동 전환합니다.

### 보안 및 안정성

- 치지직과 YouTube URL을 HTTPS, 정확한 호스트, 경로 및 영상 ID 기준으로 검증해 유사 도메인과 잘못된 링크를 차단합니다.
- 치지직/YouTube 메타데이터 요청에 제한 시간, 재시도, 리디렉션 차단 및 응답 크기 제한을 적용했습니다.
- 앱 전용 `yt-dlp` 업데이트는 공식 `SHA2-256SUMS` 체크섬과 `--version` 실행 검증을 모두 통과한 뒤에만 기존 실행 파일을 교체합니다.
- 앱 전용 `yt-dlp` 설치·업데이트를 별도 작업 스레드로 옮겨 네트워크 요청 중에도 앱 화면이 멈추지 않도록 했습니다.
- HLS 로그인 쿠키를 NAVER 도메인에만 전송하고, 외부 CDN 요청에는 전달하지 않습니다.
- HLS 재생목록과 세그먼트에 제한 시간, 재시도, 크기 제한 및 다운로드 중 취소 확인을 적용했습니다.
- `yt-dlp` 취소 시 하위 FFmpeg 프로세스까지 함께 종료해 백그라운드 작업이 남지 않도록 했습니다.
- 썸네일은 GUI를 막지 않는 별도 작업에서 HTTPS와 크기 제한을 적용해 불러옵니다.

### 검증

- 치지직 VOD `14046440`, 치지직 클립 `p7RRZ4xsws`, YouTube 영상의 실제 메타데이터 조회를 확인했습니다.
- 치지직 클립 1초 구간을 실제 다운로드해 정확한 1초 길이와 `H.264 + AAC` 스트림을 확인했습니다.
- 현재 공식 macOS `yt-dlp` 실행 파일과 배포 체크섬의 일치를 확인했습니다.
- URL 위조 차단, 클립 API 병합, 다중 조각 JSON, 도구 업데이트 보존, 쿠키 범위 및 다운로드 취소 회귀 테스트를 추가했습니다.
- 수동 HLS 병합 중 취소 시 FFmpeg 프로세스와 불완전한 임시 파일이 남지 않도록 정리했습니다.
- 전체 단위 테스트, 소스 스모크 테스트와 macOS 패키지 스모크 테스트를 수행했습니다.

## [2.0.9] - 2026-07-21

### 수정

- 앱 전용 `yt-dlp` 파일이 손상되거나 이전 Homebrew 경로를 가리켜도 실행 가능한 시스템 `yt-dlp`를 자동으로 찾아 사용하도록 수정했습니다.
- 손상된 앱 전용 `yt-dlp`가 설치 안내를 가리던 문제를 수정해 앱에서 바로 재설치할 수 있습니다.
- 출력이 없는 `yt-dlp` 프로세스를 취소할 때 GUI가 무기한 멈출 수 있던 문제를 수정했습니다.
- 쿠키가 포함된 설정 파일을 POSIX 환경에서 소유자만 읽고 쓸 수 있는 `0600` 권한으로 원자 저장합니다.
- 수동 HLS 시간 범위가 세그먼트 경계까지 포함되던 문제를 수정하고, 요청한 구간을 `H.264 + AAC`로 정밀 인코딩합니다.
- macOS Desktop 폴더의 Finder 메타데이터로 raw 앱 서명이 다시 깨지는 문제를 피하기 위해, 검증된 배포 ZIP만 최종 산출물로 남기도록 패키징 절차를 수정했습니다.

### 검증

- 손상된 앱 캐시에서 정상 시스템 `yt-dlp`로 전환되는 회귀 테스트를 추가했습니다.
- 비차단 프로세스 취소, 설정 파일 권한, HLS 정밀 트림 테스트를 추가했습니다.
- 전체 단위 테스트와 소스 및 macOS 패키지 스모크 테스트를 수행했습니다.

## [2.0.8] - 2026-07-15

### 개선

- 치지직 빠른 다시보기의 API 영상 길이는 참고값으로 표시하고, 수동 시간 범위는 실제 HLS 재생목록 길이로 최종 검증하도록 개선했습니다.
- 게시 시각 아래에 영상 길이를 별도 표시해 날짜와 길이를 혼동하지 않도록 했습니다.
- 서명 쿼리가 포함된 HLS 주소에서 화질별 fallback 주소를 잘못 만들 수 있던 문제를 수정했습니다.

### 검증

- 빠른 다시보기의 임시 API 길이보다 긴 유효 범위가 입력 단계에서 잘못 차단되지 않는지 테스트했습니다.
- 실제 HLS 길이를 벗어나는 시작 및 종료 시간이 다운로드 전에 차단되는지 테스트했습니다.
- 서명 쿼리를 유지한 HLS 화질별 주소 생성 테스트를 추가했습니다.
- 치지직 빠른 다시보기 10초 요청 구간을 실제 다운로드해 `H.264(avc1) + AAC(mp4a)`와 HLS 세그먼트 경계 범위의 정상 재생시간을 확인했습니다.
- 기존 YouTube 및 치지직 테스트와 소스 스모크 테스트를 통과했습니다.

## [2.0.7] - 2026-07-13

### 수정

- YouTube 다운로드가 MP4 컨테이너 안에 `AV1 + Opus`로 저장되어 Final Cut Pro에서 열리지 않을 수 있던 문제를 수정했습니다.
- 선택한 화질 범위 안에서 Final Cut Pro 호환성이 높은 `H.264(avc1) + AAC(m4a)` 스트림을 우선 사용합니다.
- YouTube 호스트 판별을 정확한 도메인 기준으로 강화하고, 치지직 포맷 선택에는 영향을 주지 않도록 분리했습니다.

### 검증

- 360p, 720p, 1080p YouTube 포맷 선택에서 `H.264 + AAC` 조합을 확인했습니다.
- 실제 1080p 5초 구간을 다운로드해 `H.264 High`, `yuv420p`, `AAC-LC`, MP4 출력을 확인했습니다.
- 치지직 포맷 비회귀 테스트와 패키지 스모크 테스트를 통과했습니다.
- Windows GitHub Actions가 단위 테스트와 소스 스모크 테스트를 통과한 경우에만 패키지를 빌드하도록 강화했습니다.

## [2.0.6] - 2026-07-11

### 추가

- 다운로드 센터 상단에 현재 설정된 `저장 폴더 열기` 버튼을 추가했습니다.

### 개선

- 다운로드 항목 너비가 560px보다 좁아지면 썸네일을 숨기고 작업 버튼을 축소해 핵심 정보와 동작이 계속 보이도록 개선했습니다.
- 긴 영상 제목이 고정 폭에 막히지 않고 카드 너비에 맞춰 줄바꿈되도록 개선했습니다.
- 완료 항목의 `파일 열기` 버튼은 좁은 화면에서 `열기`로 표시해 작은 창에서도 접근할 수 있습니다.

### 수정

- 작은 창에서 완료된 다운로드의 파일 열기 동작이 화면 밖으로 잘리던 문제를 수정했습니다.
- macOS 앱 정보에 배포 버전이 `0.0.0`으로 표시되던 패키징 메타데이터를 `2.0.6`으로 수정했습니다.
- macOS 빌드 스크립트가 임시 서명 검증, 패키지 스모크 테스트, 배포 ZIP 생성을 한 번에 수행하도록 수정했습니다.

### 검증

- Python 문법 검사와 소스 스모크 테스트를 통과했습니다.
- 420px 및 720px 너비에서 다운로드 카드의 반응형 표시를 회귀 테스트했습니다.
- 태그 배포 시 Windows 패키지 스모크 테스트를 GitHub Actions에서 수행합니다.

이 파일은 `2.0.6`부터 관리합니다. 이전 릴리즈 기록은 [GitHub Releases](https://github.com/ThankyouJerry/ClipCatcher/releases)에서 확인할 수 있습니다.
